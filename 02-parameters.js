//Se o usuário não digitar nenhum valor n printGreetings("Mensgame a ser passada"); por padrão será retornado undefined,
// Mas adicionando uma mansagem em printGreetings(), retornará a mensagem kkkkkkk crazyyy.

function printGreetings(message) {
  console.log(message);
}

printGreetings();
